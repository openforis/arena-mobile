import ConnectionSettings from './screen';

export const key = 'CONNECTION_SETTINGS';
export const route = 'ConnectionSettings';
export const screenConfig = {
  component: ConnectionSettings,
  options: {},
};

export default ConnectionSettings;
