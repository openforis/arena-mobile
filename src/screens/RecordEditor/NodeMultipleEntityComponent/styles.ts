import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  buttonBar: {
    alignItems: "center",
    justifyContent: "center",
    gap: 20,
  },
});
