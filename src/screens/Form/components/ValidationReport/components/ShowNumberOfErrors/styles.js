import {StyleSheet} from 'react-native';

const styles = () =>
  StyleSheet.create({
    validationReportHeader: {
      flexDirection: 'row',
      alignItems: 'center',
    },
  });

export default styles;
