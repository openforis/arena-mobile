import {StyleSheet} from 'react-native';

const styles = ({colors}) =>
  StyleSheet.create({
    listContainer: {
      flex: 1,
      backgroundColor: colors.background,
    },
  });

export default styles;
