import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textAlignLeft: {
    textAlign: 'left',
  },
  textAlignRight: {
    textAlign: 'right',
  },
});

export default styles;
