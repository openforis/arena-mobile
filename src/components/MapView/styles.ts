import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    position: "relative",
  },
  mapTypeSelector: {
    position: "absolute",
    top: 8,
    right: 8,
    zIndex: 1,
  },
});
