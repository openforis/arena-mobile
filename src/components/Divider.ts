import { Divider as RNPDivider } from "react-native-paper";

// @ts-expect-error TS(2749): 'RNPDivider' refers to a value, but is being used ... Remove this comment to see the full error message
export const Divider = () => <RNPDivider />;
