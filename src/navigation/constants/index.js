import * as Home from 'screens/Home';

export const KEYS = {
  [Home.key]: [Home.key],
};
export const SCREENS = {
  [Home.key]: Home.screenConfig,
};

export const ROUTES = {
  [Home.key]: Home.route,
};
